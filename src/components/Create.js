import { useState } from 'react';
import { useNavigate } from 'react-router-dom';

const Create = () => {
  return (
    <div className="create">
      <h2>Add a New Blog</h2>
      <p>Blog creation is disabled in this static version of the site.</p>
    </div>
  );
}

export default Create; 